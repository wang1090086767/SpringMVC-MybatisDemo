<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:mvc="http://www.springframework.org/schema/mvc"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:aop="http://www.springframework.org/schema/aop" xmlns:tx="http://www.springframework.org/schema/tx"
	xsi:schemaLocation="http://www.springframework.org/schema/beans 
		http://www.springframework.org/schema/beans/spring-beans-3.2.xsd 
		http://www.springframework.org/schema/mvc 
		http://www.springframework.org/schema/mvc/spring-mvc-3.2.xsd 
		http://www.springframework.org/schema/context 
		http://www.springframework.org/schema/context/spring-context-3.2.xsd 
		http://www.springframework.org/schema/aop 
		http://www.springframework.org/schema/aop/spring-aop-3.2.xsd 
		http://www.springframework.org/schema/tx 
		http://www.springframework.org/schema/tx/spring-tx-3.2.xsd ">
		<!--配置Handler  -->
 <bean id="ItemsController1" name="/queryItems_test.action" class="cn.example.ssm.controller.ItemsCotroller1"/>
	<!--配置另一个Handler  -->
	<bean id="ItemsController2" class="cn.example.ssm.controller.ItemsCotroller2"/> 
		<!--对于注解的Handler可以单个配置，实际开发中建议使用组件扫描 
		 -->
	<!-- <bean class="cn.example.ssm.controller.ItemsCotroller3"/> --> 
		
		<!--可以扫描controller、service、。。。。
		这里让扫描controller，指定controller的包 -->
		 <context:component-scan base-package="cn.example.ssm.controller">
		
		</context:component-scan>  
		 
		
		<!-- 处理器配置器 
		所有处理器适配器都实现HandlerAdapter接口-->
		<!-- 非注解的处理器 -->
<bean class="org.springframework.web.servlet.mvc.SimpleControllerHandlerAdapter"/>
		<bean class="org.springframework.web.servlet.mvc.HttpRequestHandlerAdapter"/>
		
		<!--非注解的 处理器映射器 
		都实现HandlerMapping的接口
		将bean的name作为url进行查找，需要在配置Handler时指定beanname（就是url）-->
		 <bean class="org.springframework.web.servlet.handler.BeanNameUrlHandlerMapping"></bean>
		<!-- 简单的url映射器 -->
		<bean class="org.springframework.web.servlet.handler.SimpleUrlHandlerMapping">
		<property name="mappings">
		<props>
		<!-- 对ItemController1进行url映射，url是Key的值  -->
		<prop key="queryItems1.action">ItemsController1</prop>
		<prop key="queryItems2.action">ItemsController1</prop>
		<prop key="queryItems3.action">ItemsController2</prop>
		</props>
		</property>
		</bean> 
		<!--注解的处理器适配器器  -->
	 <bean class="org.springframework.web.servlet.mvc.method.annotation.RequestMappingHandlerAdapter"/>
		<!--  注解的处理器映射器 -->
		<bean class="org.springframework.web.servlet.mvc.method.annotation.RequestMappingHandlerMapping"/>
		<!-- 使用 mvc:annotation-driven代替上边注解映射器和注解适配器
			mvc:annotation-driven默认加载了很多的参数绑定方法，
			比如json转换解析器就默认加载了，如果是有mvc:annotation-driven就不用配置上边的
			注解适配器和注解映射器
			实际开发中使用mvc：annotation-driven
		-->
		<!--  <mvc:annotation-driven></mvc:annotation-driven>  -->
		
		<!-- 视图解析器
		解析jsp解析，默认使用jstl标签，classpath下得有sjtl的包
		 -->
		 <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
		<!-- 配置jsp路径的前缀 -->
		 <property name="prefix" value="/WEB-INF/jsp/"/>
		 <!-- 配置jsp路径的后缀 -->
		 <property name="suffix" value=".jsp"/>
		 </bean>
		</beans>